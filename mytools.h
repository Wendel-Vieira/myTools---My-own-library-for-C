#ifndef MYTOOLS_H
#define MYTOOLS_H

#include <stdbool.h>  // Include standard headers as needed

// Function prototypes
bool isStrValidDigit(const char* str);
bool isAlphaCharacter(const char c);
int strToInt(const char* str);

#endif // MYTOOLS_H
